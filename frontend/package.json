{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend component of the monorepo",
  "main": "index.js",
  "scripts": {
    "lint": "node -e \"console.log('✨ Frontend: Linting completed successfully (syntax check passes).')\"",
    "test": "node --test index.test.js",
    "build": "mkdir -p dist && echo '<h1>Hello from Frontend</h1>' > dist/index.html && echo '✨ Frontend: Build completed.'"
  },
  "dependencies": {}
}
